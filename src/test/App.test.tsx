import { describe, expect, it } from "vitest";
//import { render, screen } from "@testing-library/react";
//import App from "../App.tsx";

describe("test the base layout of the app", () => {
  it("", () => {
    expect(1).toBe(1);
  });

  //  it("renders the game title", () => {
  //    render(<App />);
  //    const provincle = screen.getByTestId("provincle");
  //    expect(provincle).toBeTypeOf("object");
  //  });

  //  it("redirects to the current page when clicking on the game title", () => {
  //    console.log("Test case to be added");
  //  });
});
